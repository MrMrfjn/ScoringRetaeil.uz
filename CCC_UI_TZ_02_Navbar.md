# CCC UI — ТЗ 02: Навбар в стиле iOS glass

## Задачи

- [x] `nav` — glass panel: `backdrop-filter:blur(20px)`, sticky, z-index 200
- [x] `.inner` — max-width 1400px, height 56px, flex
- [x] Logo: `.logo-icon` 28px gradient block + text "CCC"
- [x] Menu items `a.lk` — chip style, 13px, rounded 8px, hover glow + translateY(-1px)
- [x] Active `.on` — glow background + accent underline (box-shadow inset)
- [x] Theme toggle — iOS capsule `.theme-toggle` with `::after` knob, translateX animation
- [x] `.usr` block — margin-left auto, 11px, muted
- [x] Sticky behavior works on all pages
- [x] All nav links clickable and correct
- [x] Alert count badge on Alerts link
